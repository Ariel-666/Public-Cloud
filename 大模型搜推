要点： 语义理解 + 内容生成 + 个性化
  搜：理解自然语言NL，会纠错，改写，意图推荐，对话式搜，生成式答
  推：语义级个性化，长文本，多模态理解，生成推荐路由，冷启动？长尾内容激活？
  一体化：融合，跨域推荐，全链路语义对齐，可解释，可交互

实现：2阶段 + RAG + 大模型重排
    1. 意图理解：大模型理解用户意图（查询/生成/问答/推荐...），纠错，改写，意图分类；
    2. 多路召回： 向量检索（查找语义相似度高的），关键词搜索（Elasticsearch实现），协同？，过滤，规则——> 得出top-key候选结果；
    3. 特征融合： 用户画像，实时行为，内容特征，上下文；
    4. LLM重排： 对候选做语义打分，排序，去重，过滤；
    5. 生成增强： 生成推荐理由，摘要，卡片式对话，多轮交互 （比如以前推荐景点，只会回复景点信息 VS 推荐3条路线 + 原因 + 交通方式等）；
    6. 合规校验： 内容安全，事实校验，幻觉抑制；
    7. 反馈闭环： 点击/转化/停留，优化模型与策略；

query/rewrite: 纠错，扩展，意图明确化，对话状态管理，多轮上下文，指代消除；
#向量检索#： embedding (向量转化，变成向量，一串维度数字，语义身份证，维度越高越准确，但存储，检索，算力更难；维度低但效果差，不准确； 一般768/1024/1536维）；向量数据库 （milvus/PGvector/FAISS?等），混合召回？
Rerank: cross-encoder? LLM打分，语义级排序，多目标优化？规则干扰
微调：fine-tuning/LoRA/QLoRA       low-rank adaption
    fine-tuning: 全部参数，最好最贵；
    LoRA: 小部分参数；监督数据（标注对 输入，输出     QA对话格式，对话、问答、客服场景）    （query-positive相关-negative不相关） 要调优的数据干净，强相关，真实场景，对齐，JSON ,格式固定
    QLoRA: 量化的基础上调小部分参数，显存要求更低了，单卡都可以调；
                   
场景痛点：
    1. 低时延高并发，50-100ms ，对GPU的利用率有要求； 向量 + LLM重排 ——> 链路长，时延高；
    2. 幻觉与事实： LLM会编造内容，需要绑定真实库 + RAG 来做事实校验 + 过滤；
    3. 实时与动态更新：内容热点数据，事实更新到LLM静态数据难，贵
    4. 数据 特征工程： 多源数据融合，格式不同，整个链路很长；
    5. QPS提高后的稳定性要求： 峰值时，对全链路高可用要求高；
    6. 冷启动？长尾内容激活？
    7. 合规，安全               

卖啥？
    1. 算力：  A10/H100/910B， 弹性伸缩AS，推理优化，蒸馏、稀疏化，算子优化，批处理；
    2. 全栈工具：


                   
